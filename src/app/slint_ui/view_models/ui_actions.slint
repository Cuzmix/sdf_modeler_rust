export enum TopBarAction {
    new-scene,
    open-project,
    save-project,
    import-mesh,
    export-mesh,
    take-screenshot,
    add-reference-image,
    toggle-all-reference-images,
    frame-all,
    focus-selected,
    undo,
    redo,
    camera-front,
    camera-top,
    camera-right,
    toggle-ortho,
    toggle-measurement,
    toggle-turntable,
    set-gizmo-translate,
    set-gizmo-rotate,
    set-gizmo-scale,
    toggle-gizmo-space,
}

export enum ToolPaletteAction {
    select,
    brush-add,
    brush-carve,
    brush-smooth,
    brush-flatten,
    brush-inflate,
    brush-grab,
}

export enum MenuCommandAction {
    new-scene,
    open-project,
    save-project,
    import-mesh,
    export-mesh,
    take-screenshot,
    add-reference-image,
    undo,
    redo,
    copy,
    paste,
    duplicate,
    delete-selected,
    frame-all,
    focus-selected,
    camera-front,
    camera-top,
    camera-right,
    toggle-ortho,
    toggle-measurement,
    toggle-turntable,
    toggle-help,
    toggle-command-palette,
}

export enum MenuNavigationAction {
    dismiss,
    select-next,
    select-previous,
    select-next-menu,
    select-previous-menu,
    activate-selected,
}

export enum SettingsCardAction {
    set-multi-axis-orientation,
    set-group-rotate-direction,
    set-multi-pivot-mode,
    set-auto-save-enabled,
    set-show-fps-overlay,
    set-continuous-repaint,
    set-auto-switch-sculpt-target-during-brush,
    export-settings,
    import-settings,
    reset-primary-shell-layout,
}

export enum PanelHeaderAction {
    close,
    pin,
    unpin,
    toggle-collapsed,
}

export enum PanelPointerPhase {
    down,
    move,
    up,
    cancel,
}

export enum PanelPointerInteractionKind {
    move,
    resize,
}

export enum PanelResizeHandle {
    none,
    top,
    right,
    bottom,
    left,
    top-left,
    top-right,
    bottom-left,
    bottom-right,
}

export struct PanelPointerAction {
    phase: PanelPointerPhase,
    interaction: PanelPointerInteractionKind,
    handle: PanelResizeHandle,
    x: float,
    y: float,
}

export enum SceneToolbarAction {
    create-sphere,
    create-box,
    create-light,
    duplicate-selected,
    delete-selected,
    start-rename-selected,
    open-node-workspace,
    open-light-workspace,
    cancel-row-drag,
}

export enum SceneRowAction {
    select-row,
    toggle-row-selection,
    toggle-row-expanded,
    toggle-row-visibility,
    toggle-row-lock,
    begin-row-rename,
    duplicate-row,
    delete-row,
    begin-row-drag,
    drop-on-row,
}

export enum SceneTextAction {
    rename-selected,
    update-rename-buffer,
    cancel-rename,
    filter-scene,
}

export enum TransformEditKind {
    position,
    rotation,
    scale,
}

export enum MaterialEditKind {
    color,
    roughness,
    metallic,
}

export enum OperationEditKind {
    smooth-k,
    steps,
    color-blend,
}

export enum SculptEditKind {
    resolution,
    layer-intensity,
    brush-radius,
    brush-strength,
}

export enum LightEditKind {
    color,
    intensity,
    range,
    cast-shadows,
    volumetric,
    volumetric-density,
}

export enum InspectorEditMode {
    nudge,
    set,
    toggle,
}

export enum RenderSettingsAction {
    set-show-grid,
    set-show-node-labels,
    set-show-bounding-box,
    set-show-light-gizmos,
    set-shadows-enabled,
    set-ao-enabled,
    nudge-export-resolution,
    set-export-resolution,
    set-adaptive-export,
    set-environment-source-procedural,
    set-environment-source-hdri,
    pick-environment-hdri,
    set-environment-rotation,
    set-environment-exposure,
    nudge-environment-bake-resolution,
    set-environment-bake-resolution,
    set-environment-background-environment,
    set-environment-background-procedural,
    set-environment-background-blur,
}

export enum ReferenceImageAction {
    toggle-reference-visibility,
    toggle-reference-lock,
    cycle-reference-plane,
    set-reference-opacity,
    set-reference-scale,
    remove-reference-image,
}

export enum ImportDialogAction {
    nudge-import-resolution,
    set-import-resolution,
    confirm-import,
    cancel-import,
}

export enum HistoryAction {
    jump-to-entry,
}

export enum WorkspaceAction {
    show-node-graph,
    show-light-graph,
    close-workspace,
    focus-selected,
    frame-all,
}
