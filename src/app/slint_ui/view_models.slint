export struct SceneRowView {
    label: string,
    selected: bool,
    hidden: bool,
    locked: bool,
}

export struct HistoryRowView {
    label: string,
    is_undo: bool,
}

export struct ReferenceRowView {
    label: string,
    plane_label: string,
    visible: bool,
    locked: bool,
    opacity: float,
    scale: float,
}

export struct ViewportPathView {
    commands: string,
    stroke: color,
    fill: color,
    stroke_width: float,
}

export enum TopBarAction {
    new-scene,
    open-project,
    save-project,
    import-mesh,
    export-mesh,
    take-screenshot,
    add-reference-image,
    toggle-all-reference-images,
    frame-all,
    focus-selected,
    undo,
    redo,
    camera-front,
    camera-top,
    camera-right,
    toggle-ortho,
    toggle-measurement,
    toggle-turntable,
    set-gizmo-translate,
    set-gizmo-rotate,
    set-gizmo-scale,
    toggle-gizmo-space,
}

export enum SceneAction {
    create-sphere,
    create-box,
    create-light,
    duplicate-selected,
    delete-selected,
    rename-selected,
    filter-scene,
    select-row,
    toggle-row-selection,
    toggle-row-visibility,
    toggle-row-lock,
    duplicate-row,
    delete-row,
}

export enum InspectorEditKind {
    position,
    rotation,
    scale,
    material-color,
    material-roughness,
    material-metallic,
    operation-smooth-k,
    operation-steps,
    operation-color-blend,
    sculpt-resolution,
    sculpt-layer-intensity,
    brush-radius,
    brush-strength,
    light-color,
    light-intensity,
    light-range,
    light-cast-shadows,
    light-volumetric,
    light-volumetric-density,
}

export enum InspectorEditMode {
    nudge,
    set,
    toggle,
}

export enum UtilityAction {
    set-render-show-grid,
    set-render-show-node-labels,
    set-render-show-bounding-box,
    set-render-show-light-gizmos,
    set-render-shadows-enabled,
    set-render-ao-enabled,
    nudge-export-resolution,
    set-export-resolution,
    set-adaptive-export,
    toggle-reference-visibility,
    toggle-reference-lock,
    cycle-reference-plane,
    set-reference-opacity,
    set-reference-scale,
    remove-reference-image,
    nudge-import-resolution,
    confirm-import,
    cancel-import,
}
