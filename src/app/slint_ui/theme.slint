export global AppTheme {
    in property <color> panel_background: #1b1e26;
    in property <color> viewport_background: #111217;
    in property <color> selected_row_background: #31435d;
    in property <color> overlay_panel_background: #171a20;
    in property <color> overlay_panel_border: #2b313d;
    in property <color> overlay_button_background: #20252f;
    in property <color> overlay_button_border: #313948;
    in property <color> overlay_button_active_background: #385074;
    in property <color> overlay_button_active_border: #5f82b3;
    in property <color> overlay_button_text: #f2f5fb;
    in property <color> overlay_divider: #2b313d;
    in property <length> frame_spacing: 8px;
    in property <length> section_spacing: 4px;
    in property <length> overlay_panel_radius: 20px;
    in property <length> overlay_button_radius: 14px;
    in property <length> overlay_panel_padding: 10px;
    in property <length> overlay_tool_spacing: 8px;
}
