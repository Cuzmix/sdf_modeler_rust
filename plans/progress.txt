Sprint initialized. No iterations completed yet.

## Iteration 1 — PRD #4: Undo/redo history unit tests
- Added 16 unit tests to `src/graph/history.rs` covering:
  - New history is empty (counts and labels)
  - Push + undo restores previous state
  - Redo restores undone state
  - Undo/redo on empty stacks returns None
  - New changes clear redo stack
  - Capacity limit (MAX_UNDO_DEPTH=50) evicts oldest entries
  - No snapshot committed during drag
  - Snapshot committed when drag ends
  - Selection changes are tracked
  - Undo/redo label ordering
  - detect_change_label: add, delete, select, edit fallback
  - Multiple undo/redo roundtrip
- Helper: `empty_scene()` creates a Scene with no nodes for predictable testing
- All 16 tests pass. cargo check, clippy, and full test suite clean.
- Files changed: `src/graph/history.rs`, `plans/prd.json`, `plans/progress.txt`

## Iteration 2 — PRD #1: Scene graph operations unit tests
- Added 46 unit tests to `src/graph/scene.rs` covering:
  - Scene::new creates default sphere
  - add_node returns incrementing IDs
  - create_primitive/operation/transform/modifier factory methods
  - next_name counter incrementing
  - remove_node patches parent references (Operation, Transform)
  - remove nonexistent returns None
  - toggle_visibility hides/unhides
  - top_level_nodes excludes children, includes orphans
  - is_descendant direct children and grandchildren
  - is_valid_drop_target: rejects same node, cycle, primitive, full operation; accepts free slot
  - reparent moves node between parents
  - detach_from_parent nulls reference
  - swap_children swaps left/right
  - set_left_child / set_right_child / set_sculpt_input
  - insert_modifier_above rewires parent chain
  - insert_transform_above rewires parent chain
  - build_parent_map maps children→parents
  - collect_subtree gathers all descendants
  - visible_topo_order: post-order, skips hidden subtrees
  - duplicate_subtree: independent copy with remapped IDs, " Copy" names
  - structure_key: changes on topology, stable on parameter change
  - data_fingerprint: changes on parameter change
  - compute_bounds: default for empty, encloses sphere, accounts for translation
  - content_eq: identical scenes, detects position change, detects hidden difference
  - NodeData::children: primitive=0, operation=2, partial operation=1
  - find_sculpt_parent returns None without sculpt ancestor
  - subtree_has_sculpt false without sculpt nodes
- Helper: `empty_scene()` creates a Scene with no nodes for predictable testing
- All 62 tests pass (16 history + 46 scene). cargo check, clippy clean.
- Files changed: `src/graph/scene.rs`, `plans/prd.json`, `plans/progress.txt`

## Iteration 3 — PRD #5: VoxelGrid unit tests
- Added 57 unit tests to `src/graph/voxel.rs` covering:
  - VoxelGrid construction: new_displacement creates zero-filled grid
  - Index layout: z-major ordering (z*res*res + y*res + x)
  - world_to_grid: maps bounds to grid edges, center maps to half-resolution
  - grid_to_world: maps edges to bounds, roundtrip consistency
  - Trilinear sampling: exact at grid corners, interpolates between values, clamps outside bounds, uniform grid returns constant
  - content_eq: identical grids, detects data/resolution/bounds changes
  - SDF primitives: sphere (center/surface/outside), box (center/face/outside), plane (above/below), cylinder, torus, capsule
  - CSG operations: union (min), subtract (max neg), intersect (max), smooth union (blends, zero-k equals hard)
  - evaluate_sdf_tree: sphere at origin/outside, missing node returns FAR, translated primitive, CSG union/subtract, single/no children operations
  - Transform nodes: translation, uniform scale, empty returns FAR
  - rotate_euler: identity, 90° Y rotation
  - Bounds estimation: single sphere, translated sphere, operation encloses both children
  - create_displacement_grid_for_subtree: centered on subtree, zero-filled
  - bake_subtree_with_progress: sphere negative inside/positive outside, progress counter, box negative at center
  - max_subtree_resolution: default without sculpt, finds sculpt node resolution
  - Sculpt evaluation: standalone samples grid, child+displacement adds correctly
  - Modifier evaluation: round, onion, mirror-X, empty returns FAR
- Helpers: `empty_scene()`, `scene_with_sphere()`, `scene_with_box()`
- All 119 tests pass (16 history + 46 scene + 57 voxel). cargo check, clippy clean.
- Files changed: `src/graph/voxel.rs`, `plans/prd.json`, `plans/progress.txt`

## Iteration 4 — PRD #2: Marching cubes mesh export unit tests
- Added 61 unit tests to `src/export.rs` covering:
  - interp_vertex: midpoint symmetric, at p0 when v0=0, at p1 when v1=0, quarter-way, equal values returns p0, 3D interpolation
  - quantize: zero, positive, negative, precision boundary, different values differ
  - collect_leaf_colors: empty scene, single primitive, skips operations
  - sample_color_at: returns closest leaf color, picks closer of two primitives, default gray when no leaves
  - marching_cubes: empty scene → no triangles, sphere produces triangles, sphere vertices near unit sphere surface, box produces triangles, vertex colors populated, progress counter advances, triangle indices valid, deduplicates shared vertices, adaptive produces similar result, higher resolution → more triangles, bounds outside geometry → no mesh, CSG union produces mesh
  - Edge/tri table sanity: all-inside/all-outside are zero, single corner has one triangle
  - OBJ writer: header + vertices, 1-indexed faces, multiple triangles, empty mesh
  - STL writer: header + triangle count, correct total size, multiple triangles
  - PLY writer: header structure, with colors, without colors, face format
  - GLB writer: magic + version, total length matches file, JSON chunk type, contains asset version, no colors omits COLOR_0, with colors has COLOR_0
  - USDA writer: header, points, face vertex counts, vertex colors, no colors omits displayColor
  - write_mesh dispatch: STL, PLY, GLB, USDA, defaults to OBJ
- Helpers: `empty_scene()`, `scene_with_sphere()`, `scene_with_box()`, `sample_mesh()`, `sample_mesh_no_colors()`
- All 180 tests pass (16 history + 46 scene + 57 voxel + 61 export). cargo check, clippy clean.
- Files changed: `src/export.rs`, `plans/prd.json`, `plans/progress.txt`
