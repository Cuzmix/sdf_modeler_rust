# Scripts

- `build_android_debug.ps1`: builds the Android debug APK with the repo's working Windows setup.
  Use `-Install` to push the APK to a single connected device, or pass `-DeviceId <serial>`
  when multiple devices are attached.
